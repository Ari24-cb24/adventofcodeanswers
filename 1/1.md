```
numbers = data.json  # see data.json
numbers = numbers.split("\n")

for i in range(len(numbers)):
  for j in range(i, len(numbers)):
      if int(numbers[i]) + int(numbers[j]) == 2020:
        print("Found number:", int(numbers[i]) * int(numbers[j]))
        break
```
